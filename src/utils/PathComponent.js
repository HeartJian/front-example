export default class PathComponent {
    constructor(fileName) {
        // 一级目录
        this.fileName = fileName;
        // 二级目录名 一个对象：{key1:"css禅意花园",key2:"js技巧"}
        this.names
        // 组件
        this.requireComponent;
        // 最后一节URL，当目录名不存在，也为二级目录名
        this.keys;

    }
}
